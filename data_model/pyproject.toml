[project]
name = "agro-data-model"
version = "0.1.0"
description = "Scripts de modelo de datos para AgroExtensión Digital"
authors = [
    {name = "AgroExtensión Digital Team"}
]
dependencies = [
    "google-cloud-firestore>=2.13.0",
    "google-auth>=2.23.0"
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
