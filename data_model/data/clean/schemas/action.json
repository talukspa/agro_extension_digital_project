{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "action.json",
  "type": "object",
  "properties": {
    "standard_code": {
      "type": "string",
      "description": "Identificador único y orientado al negocio para la pregunta/estándar (e.g., 'A001', 'P001')."
    },
    "level": {
      "type": "string",
      "description": "El nivel del estándar."
    },
    "dimension": {
      "type": "string",
      "description": "La dimensión o categoría principal a la que pertenece la pregunta."
    },
    "theme": {
      "type": "string",
      "description": "El tema específico dentro de la dimensión."
    },
    "action": {
      "type": "string",
      "description": "La acción específica tomada para cumplir con el estándar."
    },
    "good_practice": {
      "type": "string",
      "description": "Describe la buena práctica relacionada con la pregunta."
    },
    "verification_detail": {
      "type": "string",
      "description": "Describe el *contenido esperado* de la verificación (e.g., 'Registro de consumo de agua mensual')."
    },
    "verification_type": {
      "type": "string",
      "description": "El *tipo esperado* de evidencia de verificación (e.g., 'Foto', 'Archivo', 'Bitácora').",
      "enum": [
        "document",
        "image",
        "log"
      ]
    },
    "valid_answers": {
      "type": "array",
      "items": {
        "type": [
          "string",
          "number",
          "boolean"
        ]
      },
      "description": "Una lista de respuestas válidas predefinidas para esta pregunta."
    },
    "points": {
      "type": "integer",
      "description": "Puntos asociados al estándar."
    },
    "link": {
      "type": "string",
      "format": "uri",
      "description": "Una URL a información adicional o recursos relacionados con el estándar."
    },
    "resources": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "description": "El tipo de recurso (e.g., Señalética, TDR, Registro, Guía, Curso, Estándar)."
          },
          "detail": {
            "type": "string",
            "description": "Descripción detallada del recurso."
          },
          "urls": {
            "type": "object",
            "properties": {
              "web": {
                "type": "string",
                "format": "uri"
              },
              "pdf": {
                "type": "string",
                "format": "uri"
              },
              "word": {
                "type": "string",
                "format": "uri"
              },
              "excel": {
                "type": "string",
                "format": "uri"
              },
              "curso": {
                "type": "string",
                "format": "uri"
              }
            },
            "description": "Un objeto que contiene varias URLs relacionadas con el recurso, con claves que indican el tipo de URL (e.g., 'web', 'curso', 'pdf', 'word', 'excel')."
          }
        },
        "required": [
          "type",
          "detail",
          "urls"
        ]
      }
    }
  },
  "required": [
    "standard_code",
    "level",
    "dimension",
    "theme",
    "action",
    "good_practice",
    "verification_detail",
    "verification_type",
    "points"
  ]
}