{
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "Identificador único para esta instancia específica de registro/verificación."
    },
    "business_rut": {
      "type": "string",
      "description": "El RUT del negocio asociado con este registro."
    },
    "standard_code": {
      "type": "string",
      "description": "El identificador de la pregunta para la que es este registro."
    },
    "folder": {
      "type": "string",
      "description": "Ruta o identificador de la carpeta donde se almacenan los archivos asociados."
    },
    "log": {
      "type": "string",
      "description": "Un registro o bitácora de actividades relacionadas con la verificación."
    },
    "upload_timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "La fecha y hora en que se cargaron/registraron los datos de verificación."
    },
    "validation_status": {
      "type": "string",
      "description": "Estado actual de la verificación (e.g., \"pending\", \"validated\", \"rejected\")."
    },
    "validation_timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "La fecha y hora en que ocurrió la validación."
    },
    "auditor_id": {
      "type": "integer",
      "description": "El auditor que realizó la validación."
    },
    "auditor_comments": {
      "type": "string",
      "description": "Cualquier comentario proporcionado by el auditor durante la validación."
    }
  },
  "required": [
    "id",
    "business_rut",
    "standard_code"
  ]
}