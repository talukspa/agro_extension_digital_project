{
  "type": "object",
  "properties": {
    "standard_code": {
      "type": "string",
      "description": "El identificador de la pregunta que se está respondiendo."
    },
    "answer_value": {
      "type": ["string", "number", "boolean", "array", "object"],
      "description": "La respuesta real proporcionada por el usuario (el tipo de dato depende de la pregunta)."
    },
    "register_id": {
      "type": "integer",
      "description": "Enlace a la entrada de 'register' asociada para la verificación de esta respuesta."
    }
  },
  "required": [
    "standard_code",
    "answer_value"
  ]
}